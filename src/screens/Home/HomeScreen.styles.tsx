import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  addButton: {
    position: "absolute",
    bottom: 0,
    right: 10,
    width: 50,
    height: 50,
  },
});

export default styles;
